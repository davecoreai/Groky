import React from "react";

interface ThreeCanvasProps {
  isDark?: boolean;
  enabled?: boolean;
}

// Clean minimal background container (no dots across main app)
export const ThreeCanvas: React.FC<ThreeCanvasProps> = () => {
  return null;
};
